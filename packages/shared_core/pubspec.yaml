name: shared_core
description: "Shared core library for Flutter apps - theme system, utilities, error handling, BLoC base classes, and common widgets."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  logging: ^1.2.0
  path_provider: ^2.1.2
  permission_handler: ^11.2.0
  hive_flutter: ^1.1.0
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
